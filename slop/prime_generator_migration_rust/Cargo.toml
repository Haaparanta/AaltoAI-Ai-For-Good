[package]
name = "prime_generator"
version = "0.1.0"
edition = "2021"

[lib]
name = "prime_generator"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
